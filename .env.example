# ─────────────────────────────────────────────────────────────
# Supabase — projeto "Sistema" (ref: xglbppuiwdfuxdmyvbix)
# Copie este arquivo para .env e preencha com as chaves reais.
# O .env NÃO é versionado (ver .gitignore).
# ─────────────────────────────────────────────────────────────
SUPABASE_URL=https://xglbppuiwdfuxdmyvbix.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# Backend (apps/api) — usado só para regras com gate (movimentos, liberação)
API_PORT=3333

# Frontend (apps/web) — Vite só expõe variáveis com prefixo VITE_
VITE_SUPABASE_URL=https://xglbppuiwdfuxdmyvbix.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
VITE_API_URL=http://localhost:3333

# GitHub Pages — defina estes secrets no repositório:
# Settings → Secrets and variables → Actions → New repository secret
# VITE_SUPABASE_URL  e  VITE_SUPABASE_ANON_KEY
